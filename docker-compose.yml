version: "3.9"
services:
  prom:
    build: ./prometheus/.
    ports:
    - "9090:9090"
    volumes:
    - /root/vsan-prometheus/vol:/vol
  discovery:
    image: vmware/vsan-prometheus-servicediscovery:beta-0.11
    env_file:
    - vars.env
    volumes:
    - /root/vsan-prometheus/vol:/vol
